{
  "name": "basketball-stats-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "run-p dev:*",
    "build": "run-s build:*",
    "dev:css": "@tailwindcss/cli -i tailwind.css -o styles.css --watch",
    "build:css": "@tailwindcss/cli -i tailwind.css -o styles.css",
    "dev:js": "esbuild src/*.ts --bundle --target=es2020 --watch --sourcemap --outdir=scripts --servedir=.",
    "build:js": "esbuild index.ts --bundle --minify --target=es2020 --outfile=index.js",
    "format": "prettier . --write"
  },
  "keywords": [],
  "author": "Sam Johnston",
  "license": "ISC",
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "prettier": "^3.2.5",
    "tailwindcss": "^4.0.0-alpha.6",
    "typescript": "^5.4.2"
  },
  "dependencies": {
    "@faker-js/faker": "^8.4.1",
    "@tailwindcss/cli": "^4.0.0-alpha.6",
    "esbuild": "^0.20.1",
    "localforage": "^1.10.0"
  }
}

